name: ihc_jogo_da_memoria
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=2.18.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  flutter_web_plugins:
    sdk: flutter

  uuid: ^3.0.6
  cupertino_icons: ^1.0.2
  just_audio: ^0.9.30
  flutter_svg: ^1.1.5
  google_fonts: ^3.0.1
  shared_preferences: ^2.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/sounds/

  fonts:
    - family: Fredoka 
      fonts:
        - asset: assets/fonts/Fredoka-Regular.ttf